import React from 'react'

const Button = ({name}) => {
  return (
    <div>
    <div className='px-1 py-2  text-center m-3 bg-gray-200 rounded-lg'>{name}</div>
    </div>
    
  )
}

export default Button